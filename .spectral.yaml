# Spectral ruleset for CI `spec-validate` job.
# Uses the built-in OpenAPI v3 ruleset as the baseline.
# See https://meta.stoplight.io/docs/spectral/4dec24461f3af-open-api-rules
extends: ["spectral:oas"]
